<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Cold-start window background only. Compose paints everything from the first frame on;
         this exists so the launch window matches instead of flashing the platform default. -->
    <color name="medha_window_background">#FF0D0D1A</color>
</resources>
